
  # Build screen

  This is a code bundle for Build screen. The original project is available at https://www.figma.com/design/pNYM7FqDhrBqLGZ5lviDtL/Build-screen.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  